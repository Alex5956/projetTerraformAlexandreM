---
front:
  hosts:
    ${front}:
      ansible_user: debian
      ansible_become: True
backends:
  hosts:
%{for node in back_GRA ~}
    ${node}:
      ansible_user: debian
      ansible_become: True
%{endfor ~}
%{for node in back_SGB ~}
    ${node}:
      ansible_user: debian
      ansible_become: True
%{endfor ~}
